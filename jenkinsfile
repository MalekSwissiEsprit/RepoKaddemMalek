pipeline {
    agent any
    tools {
        maven 'M2_HOME' // Define the Maven tool; 'M2_HOME' should match the name configured in Jenkins
    }
    stages {
        stage('MAVEN') {
            steps {
                sh 'mvn --version' // This will print the Maven version to verify the setup
            }
        }
    }
}
